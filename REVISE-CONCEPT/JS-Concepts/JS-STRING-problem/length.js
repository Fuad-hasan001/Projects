const userName = 'koli moddin'

console.log(userName.length)

const firstNameLastChar = userName[3];
console.log(firstNameLastChar)

const whiteSpace = userName[4];
console.log(whiteSpace);

const lastNameFirstChar = userName[5];
console.log(lastNameFirstChar)
/**
 * count each string character
 * count white space
 * 
 */