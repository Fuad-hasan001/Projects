

// variable: 
const price = 12;

// variable type: 
/**
 * primitive type: Number string boolean null undefined
 * 
 * non-primitive: String, Array
 * 
 */


/**
operation: + - * / %(modulo operator)
shorthand: == === += -= ++ -- 

operations-apply: odd, even, Average, Comparison, Max, min, series, Prime, permutation, Area.
 */
const orangePrice = 36;
const afterSell = orangePrice % 5;
console.log(afterSell)

const firstName = 'hafiz';
const lastName = 'hossen';
const fullName = firstName + " " +lastName;
console.log(fullName)

const value = '12'
console.log(typeof(value))
const valueInNumber = parseInt(value);
console.log(valueInNumber);

const value2 = '3.55'
console.log(typeof(value2))
const value2InNumber = parseFloat(value2).toFixed(1)
const value2InNumberAgain = parseFloat(value2InNumber)
console.log(value2InNumberAgain)
console.log(typeof(value2InNumberAgain))