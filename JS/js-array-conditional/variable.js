//Numeric: number type variable 
var sunglassPrice = 120;

// String type variable
var specialOneName = "Ariana Grande"

// Boolean type variable (true ot false)
var isHappy = true ;

// two types of number: Integer: 120
// float, decimal: 3.65

// variable naming convention: rules, best practices
/**
 * single word
 * should use camelCase 
 * can use _ for long word
 * no gap or space, no quote(""), no keyword(var, false, true), can not start with a number(5price), can not contain dash
 */

/**
 * Math operations: + - * /
 * shorthand: += -= *= /= ++ --
 * convert: parseInt(): string to int, parseFloat(): decimal to int
 * .toFixed(2); how many numbers(1,2,..) after the decimal(.) point
 * remainder: %
 */